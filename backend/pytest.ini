[pytest]
testpaths = tests
markers =
    deepeval: marks tests as DeepEval LLM evaluation tests (may be slow and cost API credits)
    live_api: marks tests that require live API calls
pythonpath = .
